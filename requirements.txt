streamlit
requests
pillow

--- LangChain Packages ---

We only list the "top-level" packages.

'langchain' will pull in its dependencies (core, community, agents).

langchain

LangGraph is a separate package.

langgraph

The specific LLM integration.

langchain-deepseek
